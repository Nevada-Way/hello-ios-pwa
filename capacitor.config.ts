import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nevadain.helloIos',
  appName: 'hello-ios',
  webDir: 'dist/hello-ios/browser',
};

export default config;
